# Clock Creator workspace

This folder holds clock tree projects (`.json` descriptors).
Open this folder in Theia Explorer — not the application source tree.

- `clock.json` — active / default project
- Create more via **File → New Project**
