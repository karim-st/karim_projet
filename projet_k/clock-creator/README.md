# Clock Creator Extension

Theia frontend extension embedding the STM32 clock-tree editor (React Flow + Zustand + Monaco).

Open via menu **Clock Creator → Open Editor**, or the status-bar entry **MX2 Clock Creator**.
